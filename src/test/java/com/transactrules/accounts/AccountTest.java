package com.transactrules.accounts;

import com.transactrules.accounts.runtime.accounts.AccountCreatedEvent;
import com.transactrules.accounts.runtime.accounts.CreateAccountCommand;
import com.transactrules.accounts.runtime.accounts.CreateTransactionCommand;
import org.junit.Before;
import org.junit.Test;

public class AccountTest {
    /*private FixtureConfiguration fixture;

    @Before
    public void setUp() {
        fixture = new AggregateTestFixture(MyAggregate.class);
    }

    @Test
    public void testFirstFixture() {
        fixture.given(new MyEvent(1))
                .when(new CreateAccountCommand("001",1))
                .expectSuccessfulHandlerExecution()
                .expectEvents(new AccountCreatedEvent(2));
*/

}